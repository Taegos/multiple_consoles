#include <iostream>
#include "Console.h"
using namespace std;

int main(int argc, const char * argv[])
{
	//string file_mapping_location = string{ argv[1] };
	//string read_sema_name = string{ argv[2] };
	//string write_sema_name = string{ argv[3] };

	//wstring file_mapping_location_w(file_mapping_location.length(), L' '); 
	//copy(file_mapping_location.begin(), file_mapping_location.end(), file_mapping_location_w.begin()); 

	//wstring read_sema_name_w(read_sema_name.length(), L' ');
	//copy(read_sema_name.begin(), read_sema_name.end(), read_sema_name_w.begin());

	//wstring write_sema_name_w(write_sema_name.length(), L' ');
	//copy(write_sema_name.begin(), write_sema_name.end(), write_sema_name_w.begin());

	//IPCReader reader{ file_mapping_location_w, read_sema_name_w, write_sema_name_w };

	//const char * buffer = "";
	//while (reader.read(buffer)) {
	//	//cout << ch;
	//}
}